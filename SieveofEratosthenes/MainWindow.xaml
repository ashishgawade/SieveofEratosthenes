<Window x:Class="SieveofEratosthenes.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MainWindow" Height="350" Width="525" DataContext="{StaticResource test}">
    <Grid>
        <TextBlock Text="{Binding HelloMessage}"/>
        <Button Content="click" Command="{Binding TestButtonCommand}" Height="50" Width="50"/>
    </Grid>
</Window>
